<!DOCTYPE html>
<html lang="en">
  <head>
    <title>
      Salas
    </title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  <link rel="stylesheet" href="./styles/style.css">

  </head>
  <body>
    <div class="container mt-4">
      <br />
      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Senha</th>
            <th>Projetos</th>
          </tr>
        </thead>
        <tbody>
          <% if(data.length){ for(var i = 0; i< data.length; i++) {%>
          <tr>
            <td><%= data[i].nome%></td>
            <td><%= data[i].senha%></td>
            <td><a href="/projetos"><span class="no-underline">Ver projetos</span></a></td>
          </tr>
          <% } }else{ %>
          <tr>
            <td>No data ever existed.</td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </body>
</html>